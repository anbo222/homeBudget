{% include 'default/_form.html.twig' with {
    page_title: 'title.group_edit',
    page_title_param: {'%id%': group.id},
    attr: {
        method: 'post',
        action: url('groups_edit', {'id': group.id}),
    },
    back_to_list_path: 'groups_index',
} %}
