{% include 'default/_form.html.twig' with {
    page_title: 'title.operation_edit',
    page_title_param: {'%id%': operation.id},
    attr: {
        method: 'post',
        action: url('operations_edit', {'id': operation.id}),
    },
    back_to_list_path: 'operations_index',
} %}
